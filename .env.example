# Rwanda Real Estate - Docker Compose Environment Configuration
# Copy this file to .env and update with your actual values

# Domain Configuration
DOMAIN=yourdomain.com
ACME_EMAIL=admin@yourdomain.com

# PostgreSQL Configuration
POSTGRES_DB=rwanda_real_estate
POSTGRES_USER=rwanda_user
POSTGRES_PASSWORD=CHANGE_THIS_TO_SECURE_PASSWORD

# Traefik Dashboard Authentication
# Generate with: htpasswd -nb admin your_password
# Or use: echo $(htpasswd -nb admin password) | sed -e s/\\$/\\$\\$/g
TRAEFIK_DASHBOARD_AUTH=admin:$$apr1$$xyz$$abc123
